apiVersion: apps/v1
kind: Deployment
metadata:
  name: work-app
  labels:
    app: my-work-app
spec:
  selector:
    matchLabels:
      app: my-work-app
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: my-work-app
    spec:
      containers:
      - name: work-app
        image: https://nexus-registry.codikup-qa.com/registry-default/testapp:latest
        ports:
        - containerPort: 8080
      imagePullSecrets:
      - name: registry-default
