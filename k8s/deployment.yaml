apiVersion: apps/v1
kind: Deployment
metadata:
  name: work-app
  labels:
    app: my-work-app-nginx
spec:
  selector:
    matchLabels:
      app: my-work-app-nginx
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: my-work-app-nginx
    spec:
      imagePullSecrets:
      - name: registry-default
      containers:
      - name: work-app
        image: https://nexus.codikup-qa.com/repository/registry-default/v2/admin/testapp:latest
        ports:
        - containerPort: 8080