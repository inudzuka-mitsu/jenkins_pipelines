apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: work-ingress-dev
spec:
  ingressClassName: nginx
  rules:
  - host: "work-service.NAMESPACE.k8s-dev.codikup-qa.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: work-app-service-develop
            port:
              number: 80