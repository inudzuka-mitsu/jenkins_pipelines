apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: work-app-ingress
spec:
  ingressClassName: work-app
  rules:
  - host: "work-service.default.k8s-dev.codikup-qa.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: work-app-service
            port:
              number: 80