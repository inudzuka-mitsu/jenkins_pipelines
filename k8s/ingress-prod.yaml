apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress-prod
spec:
  ingressClassName: nginx
  rules:
  - host: "test-service.NAMESPACE.k8s.codikup-qa.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test-app-nginx-prod
            port:
              number: 80
